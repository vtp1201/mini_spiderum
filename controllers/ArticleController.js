const Article = require('../models/Article');

class ArticleController {
    show(req, res, next) {
        res.render('');
    }

    create(req, res, next) {
        res.render('');
    }
}

module.exports = new ArticleController();
